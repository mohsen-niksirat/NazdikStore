# راهنمای سریع بررسی NazdikStore

سلام! اگر فقط می‌خواهی نظر بدهی، لازم نیست کل کد را بخوانی.

## ۱) اجرا (۲ ترمینال)

```bat
git clone https://github.com/mohsen-niksirat/NazdikStore.git
cd NazdikStore

:: ترمینال ۱ — API
cd apps\api
npm run dev

:: ترمینال ۲ — وب
cd apps\web
npm run dev
```

در مرورگر: آدرسی که Next چاپ می‌کند (اغلب `http://127.0.0.1:3300` یا `:5000` — پورت ۳۰۰۰ روی برخی ویندوزها رزرو است).

**سلامت API:** `http://127.0.0.1:4000/api/v1/health`

اگر صفحه «API بالا نیست» دیدی یعنی ترمینال ۱ را فراموش کرده‌ای.

## ۲) مسیر دمو ۵ دقیقه‌ای

| مرحله | کجا | چه کن |
|---|---|---|
| ۱ | `/map` | شعاع ۱/۳/۵/۱۰ · فروشنده خانگی «مبهم» |
| ۲ | `/auth` | شماره `09123456789` · کد در **لاگ ترمینال API** |
| ۳ | `/vendor` | ورود دمو فروشنده · محصول · پست · موقعیت |
| ۴ | `/cart` | افزودن محصول · سفارش · پرداخت نمونه |
| ۵ | `/book` | نوبت مطب · یا «درخواست خدمت» |
| ۶ | `/rfq` | پیشنهادها · قبول قیمت · پرداخت |
| ۷ | `/admin` | ورود ادمین · آمار سفارش/کیف پول |
| ۸ | `/profile` | ویرایش نام |
| ۹ | `/messages` | چت سفارش · اعلان‌ها |

**ورود دمو** دکمه‌های آماده روی همان صفحه‌هاست (بدون SMS).

## ۳) روی چه چیزی نظر بده

- [ ] **UI/RTL** — خوانایی فارسی، دکمه‌ها، فاصله‌ها
- [ ] **نقشه** — واضح بودن پین‌ها و مبهم‌سازی خانگی
- [ ] **جریان سفارش** — سبد ← پرداخت ← وضعیت در پنل فروشنده
- [ ] **نوبت/RFQ** — آیا منطق برای کار واقعی کافی است؟
- [ ] **پیام/اعلان** — کافی است؟ چه کم دارد؟
- [ ] **امنیت/حریم خصوصی** — آیا مکان خانگی واقعاً مخفی به نظر می‌رسد؟
- [ ] **محتوای دمو** — چه چیزی جعلی/گمراه‌کننده است؟

## ۴) بازخورد

در GitHub: **Issues** → تمپلیت **Review / feedback** یا **Bug report**

لطفاً بنویس:
- چه صفحه‌ای
- چه انتظاری داشتی
- چه دیدی
- اسکرین‌شات (خیلی کمک می‌کند)

## ۵) برای کنجکاوی فنی

| سند | محتوا |
|---|---|
| `README.md` | اجرا، API، production checklist |
| `ROADMAP.md` | فازهای محصول |
| `DESIGN.md` | سیستم رنگ/تایپ |
| `Prompt 01.txt` | بریف اصلی |
| `docs/NEST_POSTGRES.md` | مسیر Nest+Postgres واقعی |
| `apps/api/test/run-phase*.cjs` | تست‌ها (بدون Docker) |

```bash
node apps/api/test/ci-pure.cjs
node apps/api/test/run-phase1.cjs
```

## نکات فنی کوتاه

- **OTP واقعی نیست** — کد در ترمینال API چاپ می‌شود.
- **پرداخت بانکی نمونه است** — درگاه Zarinpal/Saman فقط adapter آماده دارد.
- **داده‌ها in-memory** — بعد از ری‌استارت API سفارش‌ها پاک می‌شوند (except آنچه seed می‌کند).
- **Nest+Postgres** مسیر پروداکشن است؛ دمو آفلاین از mini-server استفاده می‌کند.

---

ممنون که وقت گذاشتی — بازخورد خام از بازخورد پرطمطراق بهتر است.
